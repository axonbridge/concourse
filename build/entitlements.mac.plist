<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron hardened-runtime requirements (JIT for V8, unsigned exec
         memory for the renderer, dyld vars for the app shell). -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Local HTTP API + outbound requests (AI providers, MCP servers). -->
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- Push-to-talk voice commands (whisper.cpp runs locally). -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
